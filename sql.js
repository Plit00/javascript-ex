function worker(name , age)
{
	var strName = name;
	car intRank = rank;
    student.prototype.getName() {return thus.strName;}
    student.prototype.setName(name) {this.strName = name; }
    student.prototype.getRank() {return this.setRank;}
    student.prototype.setRank(rank) {this.intRank = rank; }
}

var mycomputer = new class ({
  initialize function(name){
  	this.name = name;
  });

var mycomputer = new mac("macs");
alert(mycomputer.name);

var.cow = new class({
	initialize: function(){
		alert('cow_initialize');
	}
});
